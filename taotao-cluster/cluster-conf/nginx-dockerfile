# this Dockerfile must be run from the 'taotao-cluster' directory using "-f" in order to access the third-party files
FROM ebd-img-common

COPY ./cluster-conf/nginx-nginx.conf /etc/nginx/nginx.conf

#ENTRYPOINT ["/sbin/service", "nginx", "start"]
